<h1>footware </h1>

<div>

  {{#if logged_in}}

  <section class="card">
    <a href="/post/{{id}}">
      <header>{{title}}</header>

      <div class="card-body">
        <img src="/images/{{filename}}" alt="{{description}}">
        <p>By: {{price}}</p>
        <p>{{date}}</p>
      </div>
    </a>

    <a href="/post/{{id}}">
      <header>{{title}}</header>

      <div class="card-body">
        <img src="/images/{{filename}}" alt="{{description}}">
        <p>By: {{price}}</p>
        <p>{{date}}</p>
      </div>
    </a>

    <a href="/post/{{id}}">
      <header>{{title}}</header>

      <div class="card-body">
        <img src="/images/{{filename}}" alt="{{description}}">
        <p>By: {{price}}</p>
        <p>{{date}}</p>
      </div>
    </a>

    {{else}}
    <h3>Please login</h3>
    {{/if}}
  </section>

</div>